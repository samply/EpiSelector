version: "3.8"
services:
  app:
    image: samply/episelector-frontend:${EPISELECTOR_IMAGE_TAG:-master-de}
    pull_policy: missing
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: kontrollgruppenselektion_c
    ports:
      - '3000:80'
    stdin_open: true
    tty: true
